#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
int x,y;
float r;
printf("Enter the value of Cartesian Coordinates:\n");
scanf("%d %d",&x,&y);

r=sqrt(x*x+y*y);
0==atan(y/x);
printf ("%f",r);
return 0;
}